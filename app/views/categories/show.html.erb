<h1 align="center" style="margin-bottom: 30px;"><%= "Category: " + @category.name  %></h1>
<div align="center">
  <% if logged_in? and current_user.admin? %>
    <span class="badge" style="margin-bottom: 25px;"><%= link_to "Edit Category Name", edit_category_path(@category) %></span>
    <br></br>
  <% end %>
  <%= will_paginate @category_articles %>
</div>
<%= render 'articles/article', obj: @category_articles %>
<div align="center">
  <%= will_paginate @category_articles %>
</div>
